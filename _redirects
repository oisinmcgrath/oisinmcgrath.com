# Deploying uploads the whole repo root, dot-directories included, so the
# notes that are for whoever is working on the site — not for the public —
# are turned away here.
/CLAUDE.md      /   302
/DECISIONS.md   /   302
/.claude/*      /   302
/README.md      /   302

# Superseded pages, kept in the repo for reference but not published. The
# whole repo root is uploaded, so they have to be turned away here.
/_archive/*     /   302
